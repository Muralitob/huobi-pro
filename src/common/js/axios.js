import axios from 'axios';

class Axios {
  static post(method, url, params, options) {
    const _url = `${url}`
    return new Promise((resolve, reject) => {
      
    })
  }
}

export default axios
